language: c

env:
  - CFLAGS="-std=gnu89"
  - CFLAGS="-std=gnu99"
  - CFLAGS="-std=gnu11"

compiler:
  - clang

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake
      - cmake-data

before_script:
  - cmake --version

script:
  - mkdir cibuild
  - cd cibuild
  - cmake .. && make -j VERBOSE=1
